import { OAuthProvider } from '@crossmint/common-sdk-auth';

type OAuthUrlMap = Record<OAuthProvider, string>;
declare const useOAuthWindowListener: (oauthUrlMap: OAuthUrlMap, setError: (error: string | null) => void) => {
    createPopupAndSetupListeners: (provider: OAuthProvider, providerLoginHint?: string) => Promise<void>;
    isLoading: boolean;
    activeOAuthProvider: OAuthProvider | null;
};

export { useOAuthWindowListener };
