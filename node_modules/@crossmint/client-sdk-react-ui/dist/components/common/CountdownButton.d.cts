import * as react_jsx_runtime from 'react/jsx-runtime';
import { UIConfig } from '@crossmint/common-sdk-base';

type CountdownButtonProps = {
    initialSeconds: number;
    appearance?: UIConfig;
    countdownText: (seconds: number) => string;
    countdownCompleteText: string;
    handleOnClick: () => Promise<void>;
};
declare function CountdownButton({ initialSeconds, appearance, countdownText, countdownCompleteText, handleOnClick, }: CountdownButtonProps): react_jsx_runtime.JSX.Element;

export { CountdownButton };
