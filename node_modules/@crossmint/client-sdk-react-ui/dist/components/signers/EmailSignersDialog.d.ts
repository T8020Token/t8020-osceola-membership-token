import * as react_jsx_runtime from 'react/jsx-runtime';
import { MutableRefObject } from 'react';
import { UIConfig } from '@crossmint/common-sdk-base';

interface EmailSignersDialogProps {
    email?: string;
    open: boolean;
    setOpen: (open: boolean) => void;
    step: "initial" | "otp";
    onSubmitOTP: (token: string) => Promise<void>;
    onResendOTPCode: () => Promise<void>;
    onSubmitEmail: () => Promise<void>;
    rejectRef: MutableRefObject<((error: Error) => void) | undefined>;
    appearance?: UIConfig;
}
declare function EmailSignersDialog({ email, open, setOpen, step, onSubmitOTP, onResendOTPCode, onSubmitEmail, rejectRef, appearance, }: EmailSignersDialogProps): react_jsx_runtime.JSX.Element;

export { EmailSignersDialog };
