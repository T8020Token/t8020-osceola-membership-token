import * as react_jsx_runtime from 'react/jsx-runtime';
import { ReactNode } from 'react';
import { UIConfig } from '@crossmint/common-sdk-base';

interface BaseCodeInputProps {
    contactInfo: string;
    contactType: "email" | "phone";
    icon: ReactNode;
    title: string;
    description: ReactNode;
    helpText: string;
    onSubmitOTP: (token: string) => Promise<void>;
    onResendCode?: () => Promise<void>;
    appearance?: UIConfig;
    otpLength?: number;
}
declare function BaseCodeInput({ contactType, icon, title, description, onSubmitOTP, onResendCode, appearance, otpLength, helpText, }: BaseCodeInputProps): react_jsx_runtime.JSX.Element;

export { BaseCodeInput };
