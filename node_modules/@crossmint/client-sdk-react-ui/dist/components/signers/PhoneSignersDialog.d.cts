import * as react_jsx_runtime from 'react/jsx-runtime';
import { MutableRefObject } from 'react';
import { UIConfig } from '@crossmint/common-sdk-base';

interface PhoneSignersDialogProps {
    phone?: string;
    open: boolean;
    setOpen: (open: boolean) => void;
    step: "initial" | "otp";
    onSubmitOTP: (token: string) => Promise<void>;
    onResendOTPCode: () => Promise<void>;
    onSubmitPhone: () => Promise<void>;
    rejectRef: MutableRefObject<((error: Error) => void) | undefined>;
    appearance?: UIConfig;
}
declare function PhoneSignersDialog({ phone, open, setOpen, step, onSubmitOTP, onResendOTPCode, onSubmitPhone, rejectRef, appearance, }: PhoneSignersDialogProps): react_jsx_runtime.JSX.Element;

export { PhoneSignersDialog };
