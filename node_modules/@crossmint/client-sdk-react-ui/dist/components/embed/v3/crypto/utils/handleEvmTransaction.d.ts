import { EmbeddedCheckoutV3IFrameEmitter } from '@crossmint/client-sdk-base';
import { BlockchainIncludingTestnet } from '@crossmint/common-sdk-base';
import { Wallet } from '@dynamic-labs/sdk-react-core';

declare function handleEvmTransaction({ primaryWallet, chain, serializedTransaction, iframeClient, }: {
    primaryWallet: Wallet;
    chain: BlockchainIncludingTestnet;
    serializedTransaction: string;
    iframeClient: EmbeddedCheckoutV3IFrameEmitter;
}): Promise<void>;

export { handleEvmTransaction };
