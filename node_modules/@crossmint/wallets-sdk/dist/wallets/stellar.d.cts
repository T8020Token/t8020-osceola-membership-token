import { StellarChain, Chain } from '../chains/chains.cjs';
import { TransactionInputOptions, StellarTransactionInput, Transaction, PrepareOnly } from './types.cjs';
import { Wallet } from './wallet.cjs';
import 'viem';
import '@solana/web3.js';
import '@crossmint/client-sdk-window';
import '@crossmint/client-signers';
import 'abitype';
import '../api/types.cjs';
import '../api/gen/types.gen.cjs';
import '../signers/types.cjs';
import 'ox';
import '@crossmint/common-sdk-base';
import '../api/client.cjs';

declare class StellarWallet extends Wallet<StellarChain> {
    constructor(wallet: Wallet<StellarChain>);
    static from(wallet: Wallet<Chain>): StellarWallet;
    sendTransaction<T extends TransactionInputOptions | undefined = undefined>(params: StellarTransactionInput & {
        options?: T;
    }): Promise<Transaction<T extends PrepareOnly<true> ? true : false>>;
    private createTransaction;
}

export { StellarWallet };
