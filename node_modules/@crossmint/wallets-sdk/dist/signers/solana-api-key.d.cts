import { Signer, ApiKeyInternalSignerConfig } from './types.cjs';
import 'ox';
import '@crossmint/client-sdk-window';
import '@crossmint/client-signers';
import '@crossmint/common-sdk-base';
import '../chains/chains.cjs';
import 'viem';

declare class SolanaApiKeySigner implements Signer {
    private readonly config;
    type: "api-key";
    constructor(config: ApiKeyInternalSignerConfig);
    locator(): string;
    signMessage(): Promise<never>;
    signTransaction(): Promise<never>;
}

export { SolanaApiKeySigner };
