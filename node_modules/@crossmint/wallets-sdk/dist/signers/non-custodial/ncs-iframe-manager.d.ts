import { HandshakeParent } from '@crossmint/client-sdk-window';
import { signerOutboundEvents, signerInboundEvents } from '@crossmint/client-signers';
import { APIKeyEnvironmentPrefix } from '@crossmint/common-sdk-base';

type IframeConfig = {
    environment: APIKeyEnvironmentPrefix;
};
declare class NcsIframeManager {
    private config;
    private handshakeParent;
    constructor(config: IframeConfig);
    initialize(): Promise<HandshakeParent<typeof signerOutboundEvents, typeof signerInboundEvents>>;
    private createInvisibleIFrame;
}

export { type IframeConfig, NcsIframeManager };
