import { Signer, PasskeyInternalSignerConfig, PasskeySignResult } from './types.cjs';
import 'ox';
import '@crossmint/client-sdk-window';
import '@crossmint/client-signers';
import '@crossmint/common-sdk-base';
import '../chains/chains.cjs';
import 'viem';

declare class PasskeySigner implements Signer {
    private config;
    type: "passkey";
    id: string;
    constructor(config: PasskeyInternalSignerConfig);
    locator(): string;
    signMessage(message: string): Promise<PasskeySignResult>;
    signTransaction(transaction: string): Promise<PasskeySignResult>;
}

export { PasskeySigner };
