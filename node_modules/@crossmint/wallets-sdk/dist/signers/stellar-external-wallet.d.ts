import { Signer, ExternalWalletInternalSignerConfig } from './types.js';
import { StellarChain } from '../chains/chains.js';
import 'ox';
import '@crossmint/client-sdk-window';
import '@crossmint/client-signers';
import '@crossmint/common-sdk-base';
import 'viem';

declare class StellarExternalWalletSigner implements Signer {
    private config;
    type: "external-wallet";
    private _address;
    onSignStellarTransaction?: (payload: string) => Promise<string>;
    constructor(config: ExternalWalletInternalSignerConfig<StellarChain>);
    address(): string;
    locator(): string;
    signMessage(): Promise<never>;
    signTransaction(payload: string): Promise<{
        signature: string;
    }>;
}

export { StellarExternalWalletSigner };
