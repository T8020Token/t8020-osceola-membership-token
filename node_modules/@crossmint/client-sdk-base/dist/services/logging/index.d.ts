import { ConsoleProvider } from './ConsoleProvider.js';
import { DatadogProvider } from './DatadogProvider.js';
import './BrowserLoggerInterface.js';

type BrowserLogger = ConsoleProvider | DatadogProvider;
declare function getBrowserLogger(service: string, { onlyDatadog }?: {
    onlyDatadog?: boolean;
}): BrowserLogger;

export { type BrowserLogger, getBrowserLogger };
